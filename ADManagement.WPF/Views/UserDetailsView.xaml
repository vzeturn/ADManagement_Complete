<UserControl x:Class="ADManagement.WPF.Views.UserDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <TabControl Grid.Row="1">
            <TabItem Header="Details">
                <ScrollViewer>
                    <StackPanel Margin="12">
                        <TextBlock Text="Username" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedUser.SamAccountName}" Margin="0,0,0,8"/>

                        <TextBlock Text="Display Name" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedUser.DisplayName}" Margin="0,0,0,8"/>

                        <TextBlock Text="Email" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedUser.Email}" Margin="0,0,0,8"/>

                        <TextBlock Text="Department" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedUser.Department}" Margin="0,0,0,8"/>

                        <TextBlock Text="Title" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedUser.Title}" Margin="0,0,0,8"/>

                        <TextBlock Text="Account Status" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedUser.AccountStatus}" Margin="0,0,0,8"/>

                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Content="🔑 Reset Password" Command="{Binding ResetPasswordCommand}" Style="{StaticResource PrimaryButton}" Height="36" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Groups">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                        <Button Content="🔍 Search &amp; Add Group" Click="OpenGroupSearchDialog" Style="{StaticResource PrimaryButton}" Height="32" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                        <Button Content="🔄 Refresh" Command="{Binding LoadUserGroupsCommand}" Margin="8,0,0,0" Style="{StaticResource SecondaryButton}" Height="32" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                    </StackPanel>

                    <Grid Grid.Row="1">
                        <DataGrid ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" AutoGenerateColumns="False" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Group" Binding="{Binding}" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Background="#AAFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="⏳ Loading groups..." FontSize="18" FontWeight="Bold"/>
                                <ProgressBar IsIndeterminate="True" Width="200" Height="16" Margin="8"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button Content="➖ Remove Selected" Command="{Binding RemoveFromGroupCommand}" Style="{StaticResource SecondaryButton}" Height="32" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" Background="#F0F0F0" Padding="8" CornerRadius="4">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>